<?xml version="1.0" encoding="UTF-8"?>
<!-- 主题头像附件定义 -->
<tekinfo>
  <database>
    <table>
      <subject_icon>
        <item>
          <!-- 标识 -->
          <subject_id value="BIGINT NOT NULL PRIMARY KEY"/>

          <!-- 图标长度 -->
          <icon_length value="BIGINT DEFAULT '0' NOT NULL"/>
          <!-- 图标MIME格式 -->
          <icon_mime value="VARCHAR(64) NOT NULL"/>
          <!-- 图标修改时间 -->
          <icon_time value="BIGINT DEFAULT '0' NOT NULL"/>
        </item>
      </subject_icon>
    </table>
  </database>	
</tekinfo>
